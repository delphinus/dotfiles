---
- name: prepare config dir
  file: path=~/.config/vifm/colors state=directory

- name: prepare ptpython config dir
  file: path=~/.ptpython state=directory

- name: prepare direnv config dir
  file: path=~/.config/direnv state=directory

- name: prepare bat config dir
  file: path=~/.config/bat state=directory

- name: prepare docker config dir
  file: path=~/.docker state=directory

- name: prepare bpytop config dir
  file: path=~/.config/bpytop state=directory

- name: prepare hammerspoon config dir
  file: path=~/.hammerspoon state=directory

- name: prepare ghostty config dir
  file: path=~/.config/ghostty state=directory

- name: prepare ccstatusline config dir
  file: path=~/.config/ccstatusline state=directory

- name: prepare goneovim config dir
  file: path=~/.config/goneovim state=directory

- name: prepare 1Password directory
  file: >-
    path=~/.config/1Password
    state=directory

- name: prepare karabiner directory
  file: >-
    path='~/.config/karabiner/assets/complex_modifications'
    state=directory
  when: ansible_facts["system"] == 'Darwin'

- name: prepare hishtory config dir
  file: path=~/.hishtory state=directory

- name: make hishtory config symlinks
  file: >-
    src={{ '/'.join([dotfiles_dir, item]) }}
    dest=~/{{ item }}
    state=link
    force=yes
  with_items:
    - .hishtory/config.bash
    - .hishtory/config.fish
    - .hishtory/config.zsh

# bash / zsh は対話シェルとして使っていないのに、rc ファイルが「存在する」だけで
# インストーラの追記先にされる。LM Studio の lms bootstrap は
# .profile / .bashrc / .bash_profile / .zshrc / config.fish / .cshrc / .tcshrc を
# 順に見て、存在してかつ ~/.lmstudio/bin の絶対パスを含まないものへ PATH 行を
# 追記する (存在しないファイルは黙って飛ばす)。symlink 先が repo なので、これが
# 毎回 dotfiles の差分として現れていた。配るのをやめれば追記先が消える。
#
# .asdfrc はツール管理を asdf から mise へ移したときに不要になった。設定は
# ~/.config/mise/config.toml に移してある (legacy_version_file = yes は
# settings.idiomatic_version_file_enable_tools が相当)。asdf 本体と ~/.asdf は
# しばらく残すが、この rc を配り続けると mise と二重に効いているように見えて
# 紛らわしいので消す。
#
# .envrc / .env は 1Password Environments を使っていた頃の経路。$HOME に direnv を
# 効かせて「どこでも効く環境変数」を作っていたが、conf.d/00-env.fish と
# ~/.config/env.sh (どちらも secrets.yml の secret_env 由来) に置き換えた。
# .env は名前付きパイプで、残しておくと書き手がいないまま cat でブロックする。
#
# .config/fish/config-local.fish はどこからも source されていなかった (fish が
# 自動で読むのは conf.d だけ)。中身も lookup('env', 'DEFAULT_API_CONFIG') 由来の
# 空文字で、FJ_API_CONFIG は secret_env 側が正しい値を持っている。
#
# .config/direnv/direnv.toml は dotfiles 側に実体が無いリンク切れ。ansible が
# 張っていたものではなく手作りの残骸。direnv 自体はプロジェクトごとの .envrc で
# 使い続けるが、設定ファイルは要らない。
- name: remove dotfiles symlinks no longer distributed
  file: path=~/{{ item }} state=absent
  with_items:
    - .asdfrc
    - .bashrc
    - .zshrc
    - .envrc
    - .env
    - .config/fish/config-local.fish
    - .config/direnv/direnv.toml

- name: make dotfiles symlink
  file: >-
    src={{ '/'.join([dotfiles_dir, item]) }}
    dest=~/{{ item }}
    state=link
    force=yes
  with_items:
    - .zprofile
    - .config/1Password/ssh
    - .config/alacritty
    - .config/bat/config
    - .config/bpytop/bpytop.conf
    - .config/ccstatusline/settings.json
    - .config/efm-langserver
    - .config/ghostty/config
    - .config/fish
    - .config/gocode
    - .config/goneovim/settings.toml
    - .config/grc
    - .config/ipython
    - .config/karabiner/assets/complex_modifications
    - .config/kitty
    - .config/mise
    - .config/nvim
    - .config/nvim-dev
    - .config/peco
    - .config/powerline
    - .config/ranger
    - .config/vifm/colors/nord.vifm
    - .config/vifm/vifmrc
    - .config/yamllint
    - .editrc
    - .fdignore
    - .gitconfig
    - .gitignore-global
    - .golangci.yml
    - .grcat
    - .hammerspoon/init.lua
    - .inputrc
    - .iterm2
    - .iterm2_shell_integration.zsh
    - .jsbeautifyrc
    - .jshintrc
    - .nbrc
    - .proverc
    - .ptconfig.toml
    - .ptpython/config.py
    - .tern-project
    - .tmux
    - .tmux.conf
    - .vimrc_first.vim

- file: >-
    dest=~/.local/share/nvim/{{ item }}
    state=directory
    mode=0700
  with_items:
    - backup
    - swap
    - undo

- name: symlink dircolors
  file: >-
    src={{ dotfiles_dir }}/submodules/nord-dircolors/src/dir_colors
    dest=~/.dir_colors
    state=link
    force=yes

- git: >-
    repo=https://{{ item }}
    dest=~/git/{{ item }}
  with_items:
    - github.com/takaaki-kasai/git-foresta

- file: path=~/.ghg/bin state=directory
- file: >-
    src=~/git/github.com/takaaki-kasai/git-foresta/git-foresta
    dest=~/.ghg/bin/git-foresta
    state=link
    force=yes

# - name: prepare iTerm2 script directory
#   file: >-
#     src={{ dotfiles_dir }}/iterm2
#     dest='~/Library/Application Support/iTerm2/Scripts/Autolaunch'
#     state=link
#     force=yes
#   when: ansible_facts["system"] == 'Darwin'

- git: >-
    repo=https://{{ item }}
    dest=~/git/{{ item }}
  with_items:
    - github.com/delphinus/iterm2-imap-status
  when: ansible_facts["system"] == 'Darwin'

# - name: set up IMAP status component in iTerm2 dir
#   file: >-
#     src=~/git/github.com/delphinus/iterm2-imap-status/imap_status.py
#     dest='~/Library/Application Support/iTerm2/Scripts/Autolaunch/imap_status.py'
#     state=link
#     force=yes
#   when: ansible_facts["system"] == 'Darwin'

- name: make .ptignore file
  template: src=ptignore.j2 dest=~/.ptignore

# delphinus/kitty の kitty は Nerd Font のセル幅パッチを当てた自前ビルド。
# Homebrew には formula から .app を配置する仕組みが無い (brew linkapps は
# 削除済みで、cask だけが /Applications を触る) ので、ここで張る。opt パスは
# バージョンに依らないので張り直しは要らない。
- name: link kitty.app into ~/Applications
  when: ansible_facts["system"] == 'Darwin'
  block:
    - name: check whether delphinus/kitty/kitty is installed
      stat:
        path: /opt/homebrew/opt/kitty/kitty.app
      register: kitty_app

    - name: ensure ~/Applications exists
      file:
        path: ~/Applications
        state: directory
        mode: "0755"
      when: kitty_app.stat.exists

    - name: make kitty.app symlink
      file: >-
        src=/opt/homebrew/opt/kitty/kitty.app
        dest=~/Applications/kitty.app
        state=link
      when: kitty_app.stat.exists

# daily-sync: 毎日 brew の更新 (HEAD 系含む) を回し、無人でできないことだけを
# 通知するジョブ。詳細は bin/daily-sync の先頭コメント参照。
- name: set up daily-sync LaunchAgent
  when: ansible_facts["system"] == 'Darwin'
  block:
    - name: ensure ~/Library/LaunchAgents exists
      file:
        path: ~/Library/LaunchAgents
        state: directory
        mode: "0755"

    - name: make daily-sync LaunchAgent
      template:
        src: daily_sync.plist.j2
        dest: ~/Library/LaunchAgents/com.delphinus.daily-sync.plist
        mode: "0644"
      register: daily_sync_plist

    # plist を書き換えただけでは launchd は読み直さないので、変更時のみ
    # bootout してから bootstrap する。未ロード時の bootout は失敗するため
    # 無視する (failed_when: false)。
    - name: unload daily-sync LaunchAgent
      command: launchctl bootout gui/{{ ansible_facts["user_uid"] }}/com.delphinus.daily-sync
      when: daily_sync_plist is changed
      failed_when: false
      changed_when: true

    - name: load daily-sync LaunchAgent
      command: >-
        launchctl bootstrap gui/{{ ansible_facts["user_uid"] }}
        {{ ansible_facts["env"]["HOME"] }}/Library/LaunchAgents/com.delphinus.daily-sync.plist
      when: daily_sync_plist is changed
      changed_when: true

    # 起動 (ログイン) 契機の枠。定時の枠と引数が違う (--at-boot) ので、
    # 同じジョブには相乗りできず別ラベルにしてある。
    - name: make daily-sync-boot LaunchAgent
      template:
        src: daily_sync_boot.plist.j2
        dest: ~/Library/LaunchAgents/com.delphinus.daily-sync-boot.plist
        mode: "0644"
      register: daily_sync_boot_plist

    - name: unload daily-sync-boot LaunchAgent
      command: launchctl bootout gui/{{ ansible_facts["user_uid"] }}/com.delphinus.daily-sync-boot
      when: daily_sync_boot_plist is changed
      failed_when: false
      changed_when: true

    # bootstrap で RunAtLoad が発火するが、--at-boot が同じ起動セッションでの
    # 2 回目を弾くので、プレイブックの実行中に daily-sync が走り出すことは無い。
    - name: load daily-sync-boot LaunchAgent
      command: >-
        launchctl bootstrap gui/{{ ansible_facts["user_uid"] }}
        {{ ansible_facts["env"]["HOME"] }}/Library/LaunchAgents/com.delphinus.daily-sync-boot.plist
      when: daily_sync_boot_plist is changed
      changed_when: true

# vim:se et:

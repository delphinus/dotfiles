---
- pip:
    name:
      - Send2Trash
      - ansible-lint
      - neovim-remote
      - pynvim
      - python-language-server
      - vint
      - yamllint
    executable: pip3
    extra_args: --user
    state: latest

# vim:se ft=yaml.ansible:

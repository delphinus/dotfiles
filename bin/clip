#!/bin/bash
if nc -z localhost 8377 > /dev/null 2>&1; then
  if [[ -p /dev/stdin ]]; then
    cat - | nc localhost 8377
  elif which pbpaste > /dev/null; then
    pbpaste | nc localhost 8377
  fi
else
  echo 'clipper is not working!'
  exit 1
fi

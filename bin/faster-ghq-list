#!/bin/bash
tmpfile=$(mktemp)
ghq root --all > "$tmpfile"

{
  head -n1 "$tmpfile" | xargs -I{} find {} -mindepth 3 -maxdepth 3 -type d
  tail -n3 "$tmpfile" | xargs -I{} find {} -mindepth 1 -maxdepth 1 -type d
}

rm "$tmpfile"

#
# Executes commands at the start of an interactive session.
#

# hishtory settings
export HISHTORY_SERVER=https://hishtory.delphinus.dev
if [[ -f ~/.hishtory/config.zsh ]]; then
  source ~/.hishtory/config.zsh
fi
